class PromptBuilder:
    """Builds prompts for the LLM using retrieved chunks and chat memory."""
    def __init__(self):
        pass

    def build_prompt(self, query, retrieved_chunks, chat_memory):
        """Combine query, chunks, and memory into a prompt for the LLM."""
        pass 